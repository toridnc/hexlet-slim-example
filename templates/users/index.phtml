<!-- templates/users/index.phtml -->

<!DOCTYPE html>
<html lang="ru">

<head>
    <title>Users</title>
</head>

<body>
    <h1>Hexlet Slim Project</h1>

    <!--Buttons-->
    <a href="/">
        <button>
            About
        </button>
    </a>
    <a href="/users">
        <button>
            Users
        </button>
    </a>
    <a href="/users/new">
        <button>
            Sign in
        </button>
    </a>

    <!--Users-->
    <h1>Users</h1>
    <!--Displays a message that the user was added successfully-->
    <?php if (count($flash) > 0) : ?>
        <?php foreach ($flash as $messages) : ?>
            <?php foreach ($messages as $message) : ?>
                <i style="color: green"><?= $message ?></i>
            <?php endforeach ?>
        <?php endforeach ?>
    <?php endif ?>
    <form action="/users">
        <input type="search" name="term" value="<?= htmlspecialchars($term) ?>" placeholder="Name">
        <input type="submit" value="Search">
    </form>
    <hr>
    <!--Displays all usernames-->
    <?php foreach ($users as $user): ?>
    <div>
        <li><b><i><?= htmlspecialchars($user) ?></i></b></li>
    </div>
    <?php endforeach ?>
</body>

</html>